package com.shahrafuking.kingassistant.security

import android.app.Activity
import com.google.mlkit.vision.common.InputImage

/**
 * CameraXFrameCollector
 *
 * Placeholder/compile-time stub that should be replaced with a real CameraX ImageAnalysis
 * implementation that collects InputImage frames for the specified duration.
 */
class CameraXFrameCollector {
    fun collectFrames(activity: Activity, durationMs: Long): List<InputImage> {
        // TODO: implement CameraX ImageAnalysis collection. For now, return empty list so build succeeds.
        return emptyList()
    }
}
