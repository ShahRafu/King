# Backend environment example

# Bearer token that the Android app should use in Authorization: Bearer <token>
CLIENT_BEARER_TOKEN=replace_with_secure_token

# Optional cache/settings
CACHE_TTL_SECONDS=300
PORT=8080

# OPTIONAL: If you want to allow only certain hostnames for deep fetch, list them comma separated
ALLOWED_HOSTS=wiki.example.com,example.com
